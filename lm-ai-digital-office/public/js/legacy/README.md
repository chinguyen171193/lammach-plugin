# Disabled legacy NPC runtime

These source snapshots are retained for audit only. They are not enqueued by the
NPC test scene after the REFERENCE_CHARACTER_V1 runtime reset. They contain the
former retargeting, workstation, and procedural-avatar paths and must not be
imported by the new runtime.
