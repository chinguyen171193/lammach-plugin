# Employee 002

`employee_002_worker.fbx` is the **Worker.fbx** model selected for Employee 002, from Quaternius Ultimate Modular Women Pack → Humanoid Rigs → Individual Characters. The original pack is CC0. `employee_002.fbx` remains as the former Formal model and is no longer the bundled default.

An administrator can replace it through **AI Office → Nhân vật**. The Media Library option is preferred because it survives plugin updates.
